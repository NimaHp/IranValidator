# راهنمای مشارکت (Contributing)

**فارسی** | [English](CONTRIBUTING.en.md)

از تمایل شما برای مشارکت در پروژه IranValidator سپاسگزاریم! این راهنما مسیر مشارکت و ارسال تغییرات را برای شما شفاف و آسان می‌کند.

## پیش‌نیازها

* .NET 10 SDK
* (اختیاری) Python 3 برای اجرای اسکریپت‌های ارزیابی بنچمارک

## راه‌اندازی پروژه

مخزن را دریافت کرده و پروژه را بیلد کنید:

```bash
git clone <repository-url>
dotnet restore IranValidator.slnx
dotnet build IranValidator.slnx -c Release
```

## اجرای تست‌ها

برای اجرای مجموعه تست‌های واحد:

```bash
dotnet test IranValidator.slnx -c Release --no-build
```

* تمامی تست‌ها باید با موفقیت پاس شوند. آستانه پوشش کد **۹۵٪ (خط/شاخه/متد)** در فرایند CI توسط Coverlet ارزیابی می‌شود (ThresholdStat=total).
* جهت اجرای بنچمارک‌ها (دقیقاً مشابه دستور CI):

```bash
dotnet run -c Release --project benchmarks/IranValidator.Benchmarks -- --job medium --inprocess --filter "*X*"
```

## قواعد و اصول کدنویسی

* **مدیریت هشدارها:** تمامی پروژه‌ها از .editorconfig و تنظیم TreatWarningsAsErrors پیروی می‌کنند؛ هیچ هشداری (Warning) در بیلد پذیرفته نمی‌شود.
* **ویژگی‌های .NET:** قابلیت‌های Nullable و ImplicitUsings در سراسر پروژه فعال هستند.
* **معماری اعتبارسنج‌ها:** اعتبارسنج‌های جدید باید از الگوی **Singleton** و اینترفیس IStringValidator پیروی کنند و شامل Overloadهای Validate(string) و Validate(ReadOnlySpan<char>) باشند.
* **پرهیز از تخصیص حافظه (Zero-Alloc):** از Regex در اعتبارسنج‌ها استفاده نکنید (پردازش مستقیم روی Span انجام شود). از Boxing و پرتاب Exception برای ورودی‌های نامعتبر خودداری کنید.
* **کدهای خطا:** کدهای خطا باید از ValidationErrorCode استفاده کنند و پیام‌ها از طریق سیستم Localization (با اولویت DI و Fallback استاتیک) مدیریت شوند.
* **بهینه‌سازی پردازش:** ورودی‌ها باید از طریق Normalize(span, original) و پیش‌پردازش NeedsNormalization هدایت شوند تا در حالت عادی ۰ بایت حافظه تخصیص یابد.

## مستندات دوزبانه

زبان اصلی مستندات، فارسی است (فایل‌های بدون پسوند). مستندات انگلیسی با پسوند *.en.md مشخص می‌شوند.

* هرگونه تغییر در مستندات باید به صورت هم‌زمان در **هر دو زبان** اعمال شود.
* فایل‌های انگلیسی نباید حذف شوند.
* نوار تغییر زبان در بالای تمامی فایل‌های مستندات باید حفظ شود.

## چک‌لیست ثبت Pull Request

پیش از ارسال PR، موارد زیر را بررسی و تایید کنید:

* [ ] بیلد و تست‌ها بدون هیچ هشداری پاس می‌شوند.
* [ ] پوشش تست پروژه (حداقل ۹۵٪) حفظ شده است.
* [ ] تغییرات در بخش [Unreleased] فایل CHANGELOG.md (در هر دو زبان) ثبت شده است.
* [ ] مستندات دوزبانه در صورت نیاز به‌روزرسانی شده‌اند.
* [ ] برای تغییرات حساس به کارایی، بنچمارک اجرا شده است.
